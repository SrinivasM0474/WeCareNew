import React, { useState } from "react";
import StepperHead from "../../forms/stepper/stepper";

const Stepper = (props) => {
    return (
        <div>
            <StepperHead />
        </div>
    );
};

export default Stepper;